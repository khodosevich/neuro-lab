import { Box } from '@mui/material';

const ChatBody = () => {
	return (
		<Box sx={{
			padding: "20px",
			display: "flex",
			height: "100%",
		}}>
			ChatBody
		</Box>
	);
};

export default ChatBody;